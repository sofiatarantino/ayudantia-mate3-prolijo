
\label{sec:coordenadas_esfericas}
\vspace{0.2cm}

Para el cálculo de integrales en regiones con simetría esférica, se utiliza el sistema de coordenadas esféricas. Un punto $(x, y, z)$ se relaciona con $(\rho, \phi, \theta)$ mediante:

\begin{equation}
    \begin{cases}
        x = \rho \sin \phi \cos \theta \\
        y = \rho \sin \phi \sin \theta \\
        z = \rho \cos \phi
    \end{cases}
\end{equation}

Donde las variables se restringen a $\rho \ge 0$, $0 \le \phi \le \pi$ y $0 \le \theta \le 2\pi$ para recorrer toda la esfera.

 \textcolor{red}{se puede agregar el clasico dibubo que muestra como se transforma una parale en un cilindro con el link a GeoGebra? }


\vspace{0.4cm}
\textbf{El Determinante Jacobiano}

De acuerdo con el Teorema~\ref{teo:cambio_variable},  el cambio de coordenadas  requiere introducir el valor absoluto del determinante de la matriz Jacobiana $J_{\mathbf{T}}$ como factor de escala en el diferencial de volumen. Por lo calculado en el Ejemplo~\ref{ej:jacobiano_esferico}  y considerando que $0 \le \phi \le \pi$,  el  mismo queda:
\begin{equation}
    \left| J_{\mathbf{T}} \right| = \rho^2 \sin \phi
\end{equation}

\vspace{0.4cm}
\textbf{Diferencial de Volumen}

Dado que en el dominio de integración $\sin \phi \ge 0$ ya que $0 \le \phi \le \pi$, el factor de escala es simplemente $\rho^2 \sin \phi$. Por lo tanto, siguiendo el Teorema~\ref{teo:cambio_variable}, el diferencial de volumen resulta:

\begin{equation}
    dV = \rho^2 \sin \phi \, d\rho \, d\phi \, d\theta
\end{equation}

\textbf{Visualización del sistema de coordenadas esféricas}
\begin{figure}[H]
    \centering
    \tikzsetnextfilename{esfericas_figura}

    \begin{tikzpicture}
        \begin{axis}[
            name=ax3d,
            view={115}{22},
            width=9cm,
            height=9cm,
            xmin=-0.3, xmax=3,
            ymin=-0.3, ymax=3,
            zmin=-0.2, zmax=3,
            xlabel={$x$},
            ylabel={$y$},
            zlabel={$z$},
            axis lines=center,
            ticks=none,
            every axis x label/.style={at={(ticklabel* cs:1.05)}, anchor=north east},
            every axis y label/.style={at={(ticklabel* cs:1.05)}, anchor=north west},
            every axis z label/.style={at={(ticklabel* cs:1.05)}, anchor=south},
        ]

        % Coordenadas de P: rho=2.5, phi=50°, theta=50°
        % px = rho*sin(phi)*cos(theta) = 2.5*sin(50)*cos(50) ≈ 1.232
        % py = rho*sin(phi)*sin(theta) = 2.5*sin(50)*sin(50) ≈ 1.468
        % pz = rho*cos(phi)            = 2.5*cos(50)         ≈ 1.607

        % Segmento rho: origen -> P (línea principal)
        \addplot3[violet!80, thick, -{Stealth[length=5pt]}]
            coordinates {(0,0,0) (1.232, 1.468, 1.607)};

        % Proyección de P sobre el plano xy
        % px_xy = rho*sin(phi)*cos(theta) ≈ 1.232
        % py_xy = rho*sin(phi)*sin(theta) ≈ 1.468
        \addplot3[black!25, thin, dashed]
            coordinates {(1.232, 1.468, 0) (1.232, 1.468, 1.607)};

        % Segmento desde origen hasta proyección en xy (para mostrar theta)
        \addplot3[blue!80, thick, -{Stealth[length=5pt]}]
            coordinates {(0,0,0) (1.232, 1.468, 0)};

        % Guías punteadas finas sobre ejes
        \addplot3[black!25, thin, dashed]
            coordinates {(1.232, 1.468, 0) (1.232, 0, 0)};
        \addplot3[black!25, thin, dashed]
            coordinates {(1.232, 1.468, 0) (0, 1.468, 0)};

        % Arco theta en plano xy (de eje x hasta proyección), radio 0.85
        \addplot3[red!65, thick, domain=0:50, samples=50]
            ({0.85*cos(x)}, {0.85*sin(x)}, {0});

        % Arco phi en el plano vertical que contiene a P
        % Va desde el eje z (phi=0) hasta el segmento rho (phi=50°)
        % El plano contiene el ángulo theta=50°, radio 0.85
        \addplot3[orange!80, thick, domain=0:50, samples=50]
            ({0.85*sin(x)*cos(50)}, {0.85*sin(x)*sin(50)}, {0.85*cos(x)});

        % Punto P
        \addplot3[only marks, mark=*, mark options={fill=red!70, draw=red!80, scale=1.3}]
            coordinates {(1.232, 1.468, 1.607)};

        % Proyección de P en plano xy (punto pequeño)
        \addplot3[only marks, mark=*, mark options={fill=black!50, scale=0.7}]
            coordinates {(1.232, 1.468, 0)};

        % Etiqueta P
        \node[above right] at (axis cs: 1.232, 1.468, 1.607) {$P$};

        % Etiqueta rho (sobre el segmento diagonal)
        \node[left, color=violet!80] at (axis cs: 0.62, 0.73, 0.80) {$\rho$};

        % Etiqueta theta (ángulo azimutal en plano xy)
        \node[color=red!65] at (axis cs: 0.55, 0.28, 0.05) {$\large\theta$};

        % Etiqueta phi (ángulo polar desde eje z)
        \node[color=orange!80] at (axis cs: 0.20, 0.22, 0.55) {$\large\phi$};

        \end{axis}
    \end{tikzpicture}
    \caption{Sistema de coordenadas esféricas. El punto $P$ queda determinado 
    por $\rho \ge 0$ (distancia al origen), $\phi \in [0, \pi]$ (ángulo polar) 
    y $\theta \in [0, 2\pi)$ (ángulo azimutal).
    \href{https://www.geogebra.org/3d/pzujyjqy}{Ver en GeoGebra $\nearrow$}}
\end{figure}


\vspace{1cm}
